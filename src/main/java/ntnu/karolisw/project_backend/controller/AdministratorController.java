package ntnu.karolisw.project_backend.controller;

public class AdministratorController {
    // For student/student assistant --> get number of approved assignments necessary for a certain course
    // + getAllStudentsInQueue + get all assignments in specified course id AND return assignment + boolean approved
    // Student --> get all the assignments for a student with student id + get all courses for that student (whole object)
    // Student assistant --> get all students that has a certain course +
            // create method where input is course id and output is all students that have that course
    //queue --> method that retrieves all student in queue objects for that queue + retrieve all students
                // from queue that need approval + the same but for help
                // delete from queue, set student as waiting, set student as approved
    //

}
